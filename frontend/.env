# Existing backend config
REACT_APP_BACKEND_URL=http://localhost:8005

# Define host and port
HOST=localhost
PORT=3000

# Dynamically construct the frontend URL using the variables above
REACT_APP_FRONTEND_URL=http://${HOST}:${PORT}

# Other configs
WDS_SOCKET_PORT=443
ENABLE_HEALTH_CHECK=false
GENERATE_SOURCEMAP=false
