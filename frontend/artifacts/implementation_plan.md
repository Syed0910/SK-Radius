"# Premium Homepage Polish & Interaction Upgrade\n\nThis plan outlines the specific, high-impact enhancements required to elevate the SK Radius homepage to the standard of premium SaaS sites like Linear, Stripe, Framer, and Apple. \n\n## Proposed Changes\n\n### 1. Global Interactions & Ambient Effects\n- **Global Custom Cursor & Spotlight**: Implement a React global context that tracks mouse movement and renders a subtle, soft-glow ambient spotlight behind elements.\n- **Smooth Scroll & Parallax**: Introduce a global `useScroll` wrapper in `Home.jsx` to apply a subtle negative parallax to the background noise/grid elements as the user scrolls.\n\n---\n\n### 2. Navigation (`Navbar.jsx`)\n- **Apple-style Active States**: Replace standard hover colors with a Framer Motion `layoutId` pill that glides behind the active or hovered navigation item.\n- **Dropdown Polish**: Update the `NavigationMenu` to use `AnimatePresence` for a buttery-smooth fade-and-slide entry instead of an instant snap.\n- **Backdrop Blur**: Increase the saturation and blur radius of the scrolled navbar to achieve a true iOS-style frosted glass effect.\n\n---\n\n### 3. Hero Section (`HeroSection.jsx`)\n- **Cinematic Text Reveal**: Upgrade the headline animation from a simple fade to a character-by-character or word-by-word stagger with a blur drop-in (`filter: blur(10px) -> blur(0px)`).\n- **3D Dashboard Tilt**: Add a mouse-tracking 3D tilt effect (`perspective` and `rotateX/Y`) to the central floating dashboard mockup, similar to Stripe's hero graphics.\n\n---\n\n### 4. Bento Grid & Feature Cards (`BentoGridSection.jsx`)\n- **Refined Spotlights**: The current spotlight might be too bright on dark backgrounds. We will tune the radial gradient to a softer, color-tinted mix (e.g., `#ff6347` with 5% opacity).\n- **Border Shimmer**: Add an animated gradient border (shimmer effect) that runs continuously around the edge of the cards, masked by the border radius.\n\n---\n\n### 5. Interactive Product Showcase (`ProductShowcaseSection.jsx`)\n- **Linear-st
<truncated 1450 bytes>